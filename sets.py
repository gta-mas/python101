# set = collection which is unordered, unindexed
# duplicate values not allowed

utensils = {"fork", "spoon", "knife"}
dishes = {"bowl", "plate", "cup", "knife"}

# utensils.add("napkin")
# utensils.remove("fork")
# utensils.clear()
# utensils.update(dishes)                       # update = add all of the elements from set "dishes" to set "utensils"
# dinner_table = utensils.union(dishes)         # union = creates a whole new set

# print(dishes.difference(utensils))            # difference = compare what one set has that the other one does not have
# print(utensils.intersection(dishes))          # intersection = compare what both sets have in common


# for x in dinner_table:
#     print(x)

